<%= simple_form_for(@os_version) do |f| %>
  <% if @os_version.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@os_version.errors.count, "error") %> prohibited this os_version from being saved:</h2>

      <ul>
      <% @os_version.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.association :os_type %>
  <%= f.input :name %>
  <%= f.input :number %>
  
  <div class="actions">
    <%= f.button :submit, :label => "Create OS Versions" %>
  </div>
<% end %>