
<h1>Users</h1>
  <table class="table">
  <thead>
    <th>AndrewID</th>
    <th>Email</th>
    <th>&nbsp;</th>
  </thead>
    <% for user in @users %>
      <tr>
        <td><%= link_to user.andrew, user_path(user) %></td>
      <td><%= user.email %></td>
      <td>
        <%= link_to("Edit", edit_user_path(user))  %>&nbsp;
        <%= link_to("Delete", user_path(user), :confirm => 'Are you sure you wish to delete this information?', :method => :delete) %>
      </td>
      </tr>
  <% end %>
</table>